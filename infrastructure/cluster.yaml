apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: tienda-perritos
  region: us-east-1

managedNodeGroups:
  - name: workers
    instanceType: t3.small
    desiredCapacity: 3
    minSize: 1
    maxSize: 3